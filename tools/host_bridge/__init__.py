"""Pico-side host bridge for the PE framed protocol (plan Task 2).

Deployed to the Tiny Tapeout demo board's RP2040/RP2350 MicroPython
filesystem; ``main.py`` runs there. The modules are also importable under
CPython for the fake-HAL tests in ``tests/``.
"""
