"""Host-side tooling for the PE host controller GUI (plan: host-controller-gui).

This package is intentionally dependency-free: the core contract modules
(`protocol`, `image`) must run on a clean clone with nothing but python3, like
the rest of `tools/`.
"""
