# mapped cells and area (um2) from the last regress/synth_area.sh run.
# Refresh: ./regress/synth_area.sh | awk 'NF>=4 && $2 ~ /^[0-9]+$/ {print $1, $2, $4}'
pe_soc 4961 82893.774600
tt_um_top 5363 91268.062200
